# Web App Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: subagent-driven-development / executing-plans, task-by-task.

**Goal:** A React web dApp (`app/`) for kassandra-market — wallet connect, markets list, market detail (fund/contribute during Funding, trade YES/NO once Active, claim LP, redeem after resolution), create-market, and the activate/resolve cranks. Full MVP covering the whole lifecycle, built on `@kassandra-market/sdk`.

**Architecture:** Mirror the sibling Kassandra app `/Users/dode/Documents/solana/kassandra/app` EXACTLY — same stack, providers, data/write-action layer, wallet tx idiom, and the "Delphi" Tailwind design system (copy verbatim for an identical look). Program-agnostic files (providers, cluster, send/writeAction/useWriteAction, useAsync, ui/ design system, index.css) are copied almost verbatim; the market-specific pieces (markets enumeration, per-instruction ix-builders, phase-gated detail UI) are built fresh against our SDK.

**Stack (match sibling):** React 19, Vite 8, `@tailwindcss/vite` v4 (CSS-first, no config file — `@theme` block in `index.css`), `react-router-dom` v7, `@solana/wallet-adapter-*`, `@solana/web3.js@3.0.0-rc.2` (legacy class API), oxlint. **No react-query / no state lib** — hand-rolled `useAsync` (useEffect + nonce refetch). Fonts self-hosted via `@fontsource/*`.

**SDK surface consumed** (`@kassandra-market/sdk`, workspace:*): `MARKET_PROGRAM_ID`, `AccountType`, `MarketStatus`, `ACCOUNT_SIZES`, `EXTERNAL_PROGRAM_IDS`, program-id consts; `pda.*`; decoders `decodeConfig/decodeMarket/decodeContribution/decodeMarketOracle` (+ `Phase`, `isTerminal`, `resolvedOptionOrNull`); the 9 instruction builders; `metadao.*`; `flows.*` (`composeMarketInstructions`, `activateInstruction`, `buyInstructions`, `sellInstructions`, `redeemInstructions`, `ensureConditionalAtasInstructions`, `buildJupiterEntryRequest`, `composeWithEntry`); `associatedTokenAccount`.

**Reality of verification:** the app has no live backend in this repo, so the bar is `tsc` typecheck + `vite build` + `oxlint` all green, unit tests for pure logic (enumeration filters, view/format helpers, ix-builder wiring), and the dev server rendering without runtime errors. Full click-through against a deployed program (surfpool/validator) is a manual/next step — note it, don't fake it.

---

## Task 1: App scaffold + providers + design system + routing shell

**Files:** add `"app"` to root `pnpm-workspace.yaml`; `app/package.json`, `app/vite.config.ts`, `app/tsconfig*.json`, `app/index.html`, `app/.oxlintrc.json`; `app/src/main.tsx`, `app/src/App.tsx`, `app/src/index.css`; `app/src/providers/AppProviders.tsx`, `app/src/lib/{ClusterProvider,cluster}.ts(x)`; `app/src/components/ui/*` (copy the Delphi design system), `app/src/components/layout/Layout.tsx`, a `NavBar` (wallet connect) + `SiteFooter`; placeholder `pages/{Landing,Markets,MarketDetail,CreateMarket}.tsx`.

**Steps:**
1. Copy the sibling `app/package.json` deps VERBATIM (versions exact); rename to `@kassandra-market/app`, dep `@kassandra-market/sdk: workspace:*`. Copy `vite.config.ts` (react + tailwindcss plugins + the manualChunks splitter, retargeting the sdk chunk name), all `tsconfig*.json`, `index.html`, `.oxlintrc.json`. Add `"app"` to `pnpm-workspace.yaml`.
2. Copy `src/index.css` VERBATIM (the whole `@theme` Delphi block + wallet-adapter restyle) — identical look. Copy `providers/AppProviders.tsx` (drop the mock-mode branch), `lib/ClusterProvider.tsx`, `lib/cluster.ts` (adjust storage key to `kassandra-market:cluster`; keep localnet default + `VITE_RPC_URL`).
3. Copy the entire `components/ui/` primitives (`Button`, `Card`, `EyebrowTag`, `SectionHeader`, barrel, etc.) VERBATIM. Copy `components/layout/Layout.tsx` + a `NavBar` (wallet connect via `useWalletModal().setVisible(true)` + `useWallet()`, cluster switcher) + `SiteFooter`, adjusting brand text to "Kassandra Market".
4. `App.tsx`: `AppProviders → BrowserRouter → Routes` with lazy routes `/` (Landing), `/markets` (Markets list), `/markets/new` (CreateMarket), `/markets/:pubkey` (MarketDetail), `*`→redirect. Placeholder pages render a heading + Card so the shell is navigable.
5. `pnpm install`; `pnpm --filter @kassandra-market/app typecheck` + `build` + `lint` green; `pnpm --filter @kassandra-market/app dev` renders the shell (verify by building; a dev-server smoke check if feasible). Commit `feat(app): scaffold + providers + Delphi design system + routing shell`.

## Task 2: Data layer + read-only markets list & detail

**Files:** `app/src/data/{send,writeAction,markets}.ts`; `app/src/hooks/{useAsync,useMarkets,useMarketDetail,useKassBalance,useWriteAction}.ts`; `app/src/lib/marketView.ts` (view helpers); flesh out `pages/Markets.tsx` + `pages/MarketDetail.tsx` (read-only display).

**Steps:**
1. Copy program-agnostic infra VERBATIM from the sibling: `data/send.ts` (keypairSender + sendAndConfirm; `confirmSignature` from the SDK — ensure our SDK exports it, else copy the small poll loop into the app), `data/writeAction.ts` (the `idle→building→signing→confirming→success|error` state machine + `mapWriteError`), `hooks/useWriteAction.ts` (wallet `sendTransaction(tx, connection)` idiom — adapter fills feePayer+blockhash), `hooks/useAsync` (from `useOracles.ts`), `hooks/useKassBalance.ts`.
2. `data/markets.ts`: `enumerate` markets via `connection.getProgramAccounts(MARKET_PROGRAM_ID, { filters: memcmp(offset 0, AccountType.Market) + dataSize(ACCOUNT_SIZES.Market) })` → `decodeMarket`, skipping malformed (mirror `data/oracles.ts`). `fetchMarketDetail(pubkey)` = `getAccountInfo` + `decodeMarket`, plus child `Contribution` enumeration (memcmp the parent market pubkey at `Contribution.market` offset 8) and a read of the linked Kassandra `Oracle` (`decodeMarketOracle`) for phase/resolved_option. Include a small AMM-reserve reader (byte-offset `Amm` decoder like the sibling `data/ammV04.ts`) to compute the live YES probability = `quote/(base+quote)` when the market is Active.
3. `lib/marketView.ts`: pure helpers — status label/chip, funding progress `%` (`totalContributed/minLiquidity`), implied probability + price formatting, resolution outcome text, KASS amount formatting (9 decimals). Unit-test these.
4. `hooks/useMarkets` = `useAsync(fetchMarkets)`, `useMarketDetail(pubkey)` = `useAsync(...)` (poll every 15s when Active, like `useMarketAmms`).
5. `pages/Markets.tsx`: loading/error/empty/grid of `MarketCard`s (status chip, question placeholder from prompt hash or pubkey, funding bar or live YES%, TVL). `pages/MarketDetail.tsx`: display everything read-only — status, funding progress, probability, the MetaDAO bindings, contributions list, resolution. Client-side search/sort with `useMemo`. Commit `feat(app): data layer + read-only markets list & detail`.

## Task 3: Funding-phase write actions (create / contribute / cancel / refund)

**Files:** `app/src/data/actions/{create,contribute,cancel,refund}.ts`; `app/src/components/markets/actions/{CreateMarketForm,ContributeForm,CancelControl,RefundControl,MarketActions,WriteStatusRegion,ConnectGate,formPrimitives,amount}.tsx`; `pages/CreateMarket.tsx`.

**Steps:**
1. Copy the sibling `WriteStatusRegion`, `ConnectGate`, `formPrimitives`, `amount.ts` VERBATIM (program-agnostic form UI).
2. `data/actions/*`: each returns the ix list — derive the user KASS ATA via `associatedTokenAccount`, prepend a hand-built idempotent create-ATA ix if the ATA is missing (sibling pattern, `data: Uint8Array.of(1)`), then the SDK builder (`createMarket`/`contribute`/`cancel`/`refund`). `create` needs an oracle pubkey input (the market binds to an existing Kassandra oracle) + openYesBps + seed; validate inputs with typed `ValidationError`.
3. `MarketActions` orchestrator gates controls by status: Funding → `ContributeForm` (+ `CancelControl`/`RefundControl` when the oracle is terminal + under-funded). `CreateMarket` page = `CreateMarketForm` (oracle address, opening YES %, seed amount) → `useWriteAction`.
4. Wire `onSuccess` → `refetch()` + `refetchBalance()`. typecheck/build/lint green; unit-test the ix-builders (correct SDK builder called, ATA prepend logic). Commit `feat(app): funding-phase actions — create/contribute/cancel/refund`.

## Task 4: Active-market actions (activate / trade / claim LP / resolve / redeem)

**Files:** `app/src/data/actions/{activate,trade,claimLp,resolve,redeem}.ts`; `app/src/components/markets/actions/{ActivateControl,TradePanel,ClaimLpControl,ResolveControl,RedeemControl}.tsx`; `app/src/hooks/useMarketPrice.ts`.

**Steps:**
1. `data/actions/activate.ts`: the keeper compose→activate — call `flows.composeMarketInstructions` (returns compose ixs + refs), send those (compute-budget raised), then `flows.activateInstruction(refs)`. Expose as an `ActivateControl` (permissionless crank, shown when Funding is fully funded).
2. `data/actions/trade.ts`: `flows.ensureConditionalAtasInstructions` (prepended) + `flows.buyInstructions`/`sellInstructions` for YES/NO. `TradePanel` = outcome toggle (YES/NO), KASS amount, live price/quote display (`useMarketPrice` polling the Amm reserves → probability), buy/sell submit. (Jupiter any-token entry: wire `buildJupiterEntryRequest` behind a "pay with any token" toggle that fetches a Jupiter quote in the app and prepends the swap ix via `composeWithEntry` — or stub it with a clear TODO if the live Jupiter fetch is out of scope for this pass; note which.)
3. `claimLp/resolve/redeem` actions + controls: `ClaimLpControl` (contributor claims pro-rata LP, shown Active+), `ResolveControl` (permissionless crank once the oracle is terminal), `RedeemControl` (holder redeems winning tokens after Resolved/Void).
4. `MarketActions` gates: Active → TradePanel + ClaimLpControl + (ResolveControl when oracle terminal); Resolved/Void → RedeemControl (+ ClaimLpControl still open). typecheck/build/lint green; unit-test the action builders. Commit `feat(app): active-market actions — activate/trade/claim/resolve/redeem`.

## Task 5: Landing, polish, verification, README

**Files:** `pages/Landing.tsx`; `app/README.md`; final pass.

**Steps:**
1. `Landing.tsx`: hero + "how it works" + CTA to `/markets` (mirror the sibling landing structure/components, market-themed copy).
2. Polish: empty/loading/error states, responsive grid, wallet-connect affordances, cluster switcher, explorer links on tx success.
3. `README.md`: run instructions (`pnpm --filter @kassandra-market/app dev`, `VITE_RPC_URL`, cluster switch), the deployed-program prerequisite (surfpool/validator + a created market), and the flows the app exercises.
4. **Verify:** `pnpm --filter @kassandra-market/app typecheck && build && lint` green; run the dev server / `vite build` and confirm it compiles and the shell renders (a screenshot or dev-server smoke check). Two-stage review of the data + action layer (the SDK-integration surface). Update the design-doc status. Commit `feat(app): landing + polish + README`.

## Deferred
- Live surfpool/validator e2e click-through (needs a deployed program + seeded market) — manual next step.
- Live Jupiter HTTP integration if stubbed in Task 4.
- Position/portfolio view, market search/filters beyond basic, categorical markets UI, uneven-prior create UI.
