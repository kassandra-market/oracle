# Kassandra Functional dApp — Wallet + Read-Only Browse (slice 2) — Design + Plan

> **For Claude:** REQUIRED SUB-SKILL: subagent-driven-development (per-task implement + review).

**Goal:** Turn the slice-1 UI foundation into a functional dApp's first read slice: wire **wallet-adapter** (real Connect) + a configurable **RPC provider**, then the **read paths** — an oracle **browser** (enumerate + decode all oracles) and an oracle **detail** view (its facts, proposers, AI claims, market — all decoded via `@kassandra/sdk`). No signing yet (write flows = next slice). Prove the data layer against a **seeded surfpool/local validator** (deploy + seed real oracles → drive the read paths). All in the Delphi visual language from slice 1.

## Decisions (locked with the user)
1. **Configurable RPC; prove against a seeded surfpool/local validator.** The app reads `VITE_RPC_URL` (+ a UI cluster selector: localnet/surfpool/devnet/mainnet), default localnet. The program has NO standing deployment — a gated integration test deploys+seeds oracles on surfpool/a local validator and drives the read data layer against real on-chain bytes (the harness pattern used throughout). The UI is correct wherever it points.
2. **First functional slice = wallet + read-only browse.** Real wallet connect + RPC provider + oracle list + oracle detail (all decoded). No signing. Write flows (create/propose/vote/claim) = the NEXT slice.
3. Builds on slice 1 (`app/`, Vite+React+Tailwind-v4, the Delphi primitives + tokens). **Build the SDK first** (`pnpm --filter @kassandra/sdk build`) — the app resolves `@kassandra/sdk` types from `sdk/dist` (gitignored) — this slice DEPENDS on the SDK for real (decoders + PDA derivers), unlike slice 1's link-proof.

## Source of truth
- **SDK read surface:** `sdk/src/accounts/{oracle,fact,proposer,aiClaim,market,factVote,protocol}.ts` — `decodeX(data: Uint8Array): X` + the typed interfaces (Oracle exposes creator/kassMint/usdcMint/stakeVault/deadline/phase/phaseEndsAt/optionsCount/proposerCount/survivingCount/factCount/bondPool/settledCount/resolvedOption/openChallengeCount/thresholds/etc.). `sdk/src/accounts/common.ts` — `ACCOUNT_TYPE_OFFSET` (the account_type tag byte) + `assertAnchorAccount`/the tag check; `sdk/src/constants.ts` — `AccountType` enum, `ACCOUNT_SIZES`, `Phase` enum, `KASSANDRA_PROGRAM_ID`. `sdk/src/pda.ts` — the PDA derivers (oracle/fact/proposer/aiClaim/market/protocol). NOTE: the TS SDK has NO getProgramAccounts helper (the runner had one in Rust) — the app implements enumeration: `connection.getProgramAccounts(KASSANDRA_PROGRAM_ID, { filters: [{ memcmp: { offset: ACCOUNT_TYPE_OFFSET, bytes: <base58 of the tag byte> } }] OR { dataSize: ACCOUNT_SIZES.Oracle }] })` → `decodeOracle(acct.data)`.
- **web3.js:** `@solana/web3.js` 3.0.0-rc.2 (classic-style `Connection`, `PublicKey`, `getProgramAccounts`, `getAccountInfo`, `getMultipleAccountsInfo`). Wallet-adapter deps already installed in slice 1 (`@solana/wallet-adapter-react`/`-base`/`-react-ui`/`-wallets`) — NOT yet wired.
- **Slice-1 foundation to reuse:** `app/src/components/ui/*` (Card, EyebrowTag, SectionHeader, Button, AvatarBubble, TriggerPreviewCard, etc.), the `@theme` tokens (`app/src/index.css`), the router (`app/src/main.tsx` — add `/oracles` + `/oracles/:pubkey`), `app/src/components/landing/NavBar.tsx` (the Connect placeholder → make it real). The surfpool harness pattern: `sdk/test/surfpool/harness.ts` (deploy the program via setAccount, fabricate SPL, seed accounts, RPC).
- **Seeding real oracles:** reuse how the SDK E2Es create oracles (`sdk/test/surfpool/lifecycle-e2e.test.ts` / `settlement-e2e.test.ts` — `initProtocol` + `createOracle` + `propose` etc.) to seed a few oracles in varied phases for the browse test.

## Tasks

### FA1 — Wallet-adapter + RPC provider + app shell
- **RPC/cluster provider** (`app/src/providers/` or `app/src/lib/`): a `ConnectionProvider`-backed context reading `VITE_RPC_URL` (default `http://127.0.0.1:8899`) with a UI **cluster selector** (localnet / surfpool[same localnet default] / devnet / mainnet-beta — endpoints configurable; persist the choice in localStorage). Expose a typed `useConnection()`/`useCluster()`.
- **Wallet-adapter:** wrap the app in `ConnectionProvider` + `WalletProvider` (+ `WalletModalProvider` from `-react-ui`) with a sensible wallet set (e.g. Phantom/Solflare via `-wallets`, or the standard-wallet autodetect). Make the NavBar **Connect** button REAL (open the wallet modal / show the connected address + a disconnect) — restyle the wallet-adapter button to the Delphi NavPill look (soft-cream, sepia, 12px) OR wrap the adapter's connect in the existing NavPill. Import the adapter's CSS but override to match Delphi (no default purple).
- **App shell:** the router gains authenticated-agnostic routes; a shared layout (the NavBar + a footer) wraps `/`, `/oracles`, `/oracles/:pubkey`, `/styleguide`. The landing stays at `/`. Keep it read-only (no tx yet). No console errors; the connect flow works (verify with the wallet modal opening — a real wallet needn't be installed to prove the modal + provider wiring).
- `pnpm --filter @kassandra/sdk build` (SDK first) + `pnpm --filter app typecheck` + `build` (green + `verify-css OK`) + `lint`. Commit `feat(app): wallet-adapter + configurable RPC provider + app shell`.

### FA2 — Oracle read data layer + gated surfpool integration test
- **Data layer** (`app/src/data/` — pure, testable functions taking a `Connection`, NO React): 
  - `fetchOracles(connection): Promise<{ pubkey, oracle: Oracle }[]>` — `getProgramAccounts(KASSANDRA_PROGRAM_ID, { filters: [account_type == Oracle] })` (memcmp on `ACCOUNT_TYPE_OFFSET`, and/or `dataSize: ACCOUNT_SIZES.Oracle`), decode each with `decodeOracle`, skip/annotate any that fail the tag/size check. Sort (e.g. by deadline desc). 
  - `fetchOracleDetail(connection, oraclePubkey): Promise<{ oracle, facts: Fact[], proposers: Proposer[], aiClaims: AiClaim[], market?: Market }>` — fetch the Oracle, then enumerate its children (getProgramAccounts filtered by account_type + a memcmp on the child's `oracle` field offset — determine each child's oracle-field offset from the SDK interface/layout, mirroring the runner's Fact.oracle-offset approach; OR derive known PDAs where deterministic). Decode all.
  - Handle RPC errors + empty results gracefully (typed results, no throws that crash the UI).
- **Gated integration test** (`app/test/oracle-data.e2e.test.ts` or under a gated dir; `KASSANDRA_E2E=1`): boot surfpool/a local validator, deploy the program, `initProtocol`, seed 2–3 oracles in varied phases (+ a fact/proposer/aiClaim on one) using the SDK builders (mirror the SDK E2E seeding). Run `fetchOracles` + `fetchOracleDetail` against the real cluster over RPC → ASSERT the decoded oracles match what was seeded (count, phases, the child facts/proposers/claims). This proves the read data layer against REAL on-chain bytes. Keep the default `pnpm --filter app test` (if any) offline; gate the surfpool test. (Reuse `sdk/test/surfpool/harness.ts` — import it, or replicate the minimal deploy+seed.)
- Unit tests (offline): `fetchOracles`/`fetchOracleDetail` against a MOCK connection (canned getProgramAccounts/getAccountInfo returning real decoded-account byte fixtures) — assert enumeration + decode + the tag-mismatch skip. 
- `pnpm --filter app typecheck` + `build` + `lint` green + the offline unit tests pass. Commit `feat(app): oracle read data layer + gated surfpool integration test`.

### FA3 — Browse UI: oracle list + oracle detail (Delphi style)
- **Oracle list** (`app/src/pages/Oracles.tsx` at `/oracles`): consume `fetchOracles` via a small query hook (loading/error/empty states). Render each oracle as a Delphi `Card` in a responsive grid: the question/prompt (or a prompt-hash short + a human label if we only have the hash — show the hash truncated + the phase), an `EyebrowTag`/status chip for the `Phase` (Proposal/Fact/AiClaim/Challenge/Resolved/InvalidDeadend — map phase → a label + an on-brand tone; use ember ONLY for a punchy status like "Challenged"), the deadline (relative), proposer/fact counts, and the resolved option if Resolved. A `SectionHeader` intro. Click → detail.
- **Oracle detail** (`app/src/pages/OracleDetail.tsx` at `/oracles/:pubkey`): consume `fetchOracleDetail`. Show the oracle's full state (phase, deadline, options, bond pool, thresholds — laid out editorially, NOT a raw JSON dump), then sections for Facts (each: content-hash short, agreed/rejected, votes), Proposers (each: authority short, option, bond, correct/flipped/disqualified), AI Claims (each: submitter, option, model/params/io hashes short), and the Market if present (settle state). Use `Card`/`TriggerPreviewCard`/`AvatarBubble` (initials from pubkeys). Truncate pubkeys/hashes with a copy-on-click. Loading/error/not-found states.
- Delphi-faithful throughout (parchment canvas, chestnut-only fill, flat + hairlines, serif ≥20px, radii vocabulary, ember sparingly for 1–2 status punctuation moments, AA contrast). Responsive (375/1280). Accessible (landmarks, heading hierarchy, focus-visible, aria on status chips). Wire the routes; the NavBar links to `/oracles`.
- **Render verification:** since there's no standing deployment, render the UI with (a) MOCK data (a fixture set of decoded oracles) in a dev/story mode so the list/detail are visually reviewable offline, AND (b) if feasible, point a preview at the seeded surfpool from FA2 and confirm it lists the real seeded oracles. Verify with a headless browser at 375 + 1280 (screenshots + no console errors), like the slice-1 landing review.
- `pnpm --filter app typecheck` + `build` (+ `verify-css OK`) + `lint` green. Update `app/README.md` (the browse routes, the RPC/cluster config, how to point at a seeded surfpool). Commit `feat(app): oracle browser + detail views (read-only, Delphi style)`.

## Out of scope / deferred (next slice)
- WRITE flows: create oracle, propose, submit/vote fact, submit AI claim, finalize, open/settle challenge, claim/close/sweep — all signed by the wallet. (Builders exist in the SDK; wallet is wired here; the write UI + tx-confirm UX is the next slice.)
- A standing devnet/mainnet deployment (the app is configurable to point at one when it exists).
- Any program/SDK change (the app only consumes the built SDK).
- Real-time subscriptions / websocket account-change streaming (poll/refetch is fine for slice 2).

## Delta log

### FA1 — Wallet-adapter + configurable RPC provider + app shell (done)
- **Cluster/RPC provider** (`app/src/lib/cluster.ts` + `app/src/lib/ClusterProvider.tsx`): `Cluster` enum `localnet | devnet | mainnet-beta`; `endpointFor()` resolves endpoints (localnet = `VITE_RPC_URL` ?? `http://127.0.0.1:8899`, devnet/mainnet = public RPC). Choice persisted in `localStorage` (`kassandra:cluster`). `ClusterProvider` holds the cluster state and wraps children in wallet-adapter's `ConnectionProvider` keyed on the resolved `endpoint`, so the memoized `Connection` from `useConnection()` updates reactively on cluster change. `useCluster()` exposes `{ cluster, endpoint, setCluster, clusters }`. (Split into `.ts` non-component module + `.tsx` provider to keep oxlint fast-refresh warning-free.)
- **Wallet-adapter nesting** (`app/src/providers/AppProviders.tsx`): `ClusterProvider` (→ `ConnectionProvider`) > `WalletProvider` (autoConnect; standard-wallet autodetect + explicit Phantom/Solflare adapters) > `WalletModalProvider` > app. Read-only — nothing is signed/sent.
- **Delphi restyle:** `@solana/wallet-adapter-react-ui/styles.css` is `@import`ed at the top of `index.css` (after `tailwindcss`) so the following override rules reliably win the cascade — modal wrapper → parchment, serif sepia title, soft-cream wallet rows with chestnut hover. No purple leaks (the only remaining `#512da8` is on `.wallet-adapter-button-trigger`, which is never rendered — the NavPill drives the modal).
- **Real NavBar Connect** (`NavBar.tsx`): disconnected → NavPill "Connect wallet" opens the modal via `useWalletModal().setVisible(true)`; connecting → "Connecting…"; connected → NavPill showing the truncated address (`Abc1…Xy9z`), click to disconnect. Added a Delphi `<select>` cluster selector + an "Oracles" route link; wordmark/links use react-router `Link`.
- **App shell:** `app/src/components/layout/Layout.tsx` (NavBar + `<Outlet/>` + SiteFooter) wraps all routes. Router (`main.tsx`) now: `/` (Landing), `/oracles` + `/oracles/:pubkey` (FA3 stub pages), `/styleguide`, `*` → `/`. Landing refactored to render only its `<main>` content (chrome moved to Layout).
- **Verify:** `pnpm --filter @kassandra/sdk build` (SDK-first) green; `@kassandra/sdk` imports resolve (`KASSANDRA_PROGRAM_ID` OK). app typecheck / build (+ `verify-css OK`) / lint all green. `vite preview` serves `/`, `/oracles`, `/oracles/:pubkey`, `/styleguide` → 200. Modal wiring verified statically (providers in bundle; Delphi override rules confirmed to win the CSS cascade over the adapter defaults) — no headless browser available in-env.

### FA2 — Oracle read data layer + gated surfpool integration test (done)
- **Data layer** (`app/src/data/oracles.ts` — pure, no React): `fetchOracles(connection): Promise<OracleSummary[]>` (`OracleSummary = { pubkey: string; oracle: Oracle }`) enumerates via `getProgramAccounts(KASSANDRA_PROGRAM_ID, { filters: [{ memcmp: { offset: ACCOUNT_TYPE_OFFSET(0), bytes: <base58 tag byte> } }, { dataSize: ACCOUNT_SIZES.Oracle }] })`, `decodeOracle`s each, SKIPS any that fail the SDK decoder's tag/size guard (per-account try/catch), sorts by `deadline` desc. `fetchOracleDetail(connection, oraclePubkey): Promise<OracleDetail>` fetches the Oracle (`getAccountInfo` + `decodeOracle`; throws typed `OracleNotFoundError` if absent/wrong-type), then enumerates each child type in parallel filtered by `account_type` tag + `{ dataSize }` + a `memcmp` on the child's parent-`oracle` pubkey at **offset 8** (`CHILD_ORACLE_OFFSET` — Fact/Proposer/AiClaim/Market all store `oracle` as the first field after the 8-byte header; verified against the SDK decoders). Market is enumerated by the same oracle memcmp; the first is surfaced as `market?` (`undefined` when none). A small `base58Encode` handles the single account_type tag byte (web3.js exports no byte-array base58 encoder; the child oracle memcmp reuses the address's own base58 string). Robust: bad blobs skipped (never thrown), missing oracle → typed error, missing market → undefined.
- **Offline unit tests** (`app/test/oracles.unit.test.ts`, 7 tests, default suite): a MOCK `Connection` returns canned accounts built from REAL Pod byte layouts (buffers of the pinned ABI size, tag @0, fields at their exact offsets — deadline @136, phase @161, child oracle @8, etc.). The mock models a permissive RPC (honours `memcmp`, ignores `dataSize`) so a malformed blob (Oracle tag + wrong 300-byte size) reaches the decoder and proves the SKIP guard (excluded, not thrown). Asserts: enumerates + decodes N oracles, sorts by deadline desc, skips the bad blob; detail assembles oracle + fact + proposer + aiClaim + market scoped by the oracle memcmp (a different-oracle fact is excluded), handles missing market + empty children, and `OracleNotFoundError` for absent/wrong-type.
- **Gated surfpool integration test** (`app/test/oracle-data.e2e.test.ts`, `KASSANDRA_E2E=1`): boots surfpool (port 8903), deploys the `.so`, `init_protocol`, and SEEDS 3 oracles with REAL instructions over RPC (reusing `sdk/test/surfpool/harness.ts` imported by relative path) — nonce 1 left in `Proposal`; nonce 2 uncontested → `Resolved(option 1)`; nonce 3 driven through the dispute chain (propose×2 conflicting → finalize_proposals → submit_fact → advance_phase → vote_fact → finalize_facts → submit_ai_claim) → `AiClaim` with 1 fact + 2 proposers + 1 AI claim. Then `fetchOracles`/`fetchOracleDetail` read them back over RPC and assert the decoded phases/resolvedOption/deadline-desc-sort and the detailed oracle's fact (uri `ipfs://seeded-fact`), both proposer options {0,1}, and the AI claim (option 0, model_id 0x11×32). Runtime ~2.6s (surfpool transaction-mode + instant time-travel). Needed `resolve: { dedupe: ["@solana/web3.js"] }` in the app vitest config — the app's Keypair Address and the SDK's `new Address(...)` guard use two workspace copies of web3.js otherwise (branded-type check throws across duplicate module instances).
- **Config/scripts:** added `app/vitest.config.ts` (default suite includes `test/**/*.test.ts`, excludes `*.e2e.test.ts` unless `KASSANDRA_E2E=1`), `test`/`test:e2e` scripts + the `vitest` devDep to `app/package.json`.
- **Verify:** `pnpm --filter @kassandra/sdk build` (SDK-first) + `pnpm install` green; app typecheck / build (+ `verify-css OK`) / lint green; default `pnpm --filter app test` = 7 unit tests offline green (e2e excluded); gated `KASSANDRA_E2E=1 pnpm --filter app test` = 10 passed (7 unit + 3 surfpool) against a real seeded cluster. No React/UI; no FA1-file collision (only `app/src/data/*`, `app/test/*`, `app/vitest.config.ts`, `app/package.json`).

### FA3 — Browse UI: oracle list + oracle detail (Delphi style) (done)
- **Query hook** (`app/src/hooks/useOracles.ts`): a tiny `useAsync` (useEffect + state, NO TanStack Query) with `{ data, loading, error, refetch }`, an unmount/superseded-run guard (`active` flag + `taskRef`), and a nonce-driven `refetch`. `useOracles()` and `useOracleDetail(pubkey)` pull the `Connection` from FA1's `useConnection()` and call the FA2 `fetchOracles`/`fetchOracleDetail`; deps include `connection`, so a cluster switch (new endpoint → new memoized Connection) re-fetches automatically. In mock mode they resolve fixtures WITHOUT touching the connection (real path untouched).
- **Presentation helpers** (`app/src/lib/oracleView.ts`, pure): `phaseView(Phase) → { label, tone }` (Proposal/FactProposal/FactVoting/AiClaim/FinalRecompute = `neutral` sepia/bronze; **Challenge = `ember` "Challenged"** — the one spark; Resolved = `confirmed` calm chestnut; InvalidDeadend/unknown = `muted` stone); `relativeDeadline(bigint)` hand-rolled from unix seconds ("ends in 3d" / "ended 2d ago", d/h/m/s, no date lib); `truncateMiddle`, `hashPreview` (`0x1a2b…c3d4`) / `hashHex`, `groupDigits` (base-unit thousands separators), `windowLabel`.
- **Shared components** (`app/src/components/oracles/`): `Chip` (tones neutral/ember/confirmed/muted, hairline + 8px radius, label always real text); `PhaseChip` (wraps `phaseView`, `aria-label="Phase: …"`); `Truncated` (`value`/`copyable`/`label` — copy-on-click via `navigator.clipboard`, transient "✓ copied", labelled button, focus-visible, no ember).
- **Oracle list** (`app/src/pages/Oracles.tsx` at `/oracles`): `SectionHeader` intro (`as="h1"` — added an optional heading-level prop to the slice-1 `SectionHeader`, default `h2`, so the page owns its sole h1) over a responsive `Card` grid (1-col @375 → 2 @sm → 3 @lg). Each card: `PhaseChip`, relative deadline, phase label as the serif headline, prompt-hash preview (mono), proposer/fact/option counts, and "Resolved · option N" (or "no valid option") when Resolved. Card is a `<Link>` (preserves `?mock` search). States: loading ("Reading the chain…" + 6 skeleton cards), error (Delphi card + message + `refetch` Retry), empty ("No oracles found on <ClusterLabel>").
- **Oracle detail** (`app/src/pages/OracleDetail.tsx` at `/oracles/:pubkey`): editorial layout — header (eyebrow + h1 "Oracle dispute" + `PhaseChip` + relative deadline + copyable oracle pubkey + prompt-hash + resolved-option line); a 6-tile stats grid (options/proposers/surviving/facts/settled/open-challenges, 2-col @375 → 3 @sm); a bond-pool card (raw grouped base units, annotated "KASS base units (raw, unscaled)" + dispute-bond total); a Parameters card (thresholds as `n / d`, phase/proposal/twap windows readable); then sections **Facts** (agreed/rejected + duplicate/settled chips, content-hash + fact-account copyable, approve/duplicate stake, **uri rendered as inert untrusted text — never linked/fetched**), **Proposers** (`AvatarBubble` initials from authority, disqualified/slashed/flipped/AI-finalized/Held chips, option `orig → claim`, bond, slashed amount), **AI claims** (option + submitter, `challenged` = ember "Challenged" chip, model/params/io hashes as TriggerPreview-style cream sub-cards with copyable truncations), **Challenge market** (settled/open, challenger + USDC + TWAP + question), **Accounts** (creator/mints/stake-vault copyable). Empty sections render a driftwood note. States: loading, `OracleNotFoundError` → friendly "Oracle not found on <ClusterLabel>", generic error + Retry.
- **Mock affordance** (`app/src/data/mockOracles.ts`, does NOT pollute the real path): `isMockMode()` (`VITE_MOCK=1` build-time OR `?mock` runtime query param); 5 decoded-shaped `OracleSummary` fixtures (one per phase incl. the ember Challenged) + `mockOracleDetail(pubkey)` (the challenged oracle gets a full child set: 2 facts, 2 proposers, 1 AI claim, 1 market; others empty; unknown pubkey → `OracleNotFoundError` to exercise not-found). Addresses are readable casts (display-only; never fed to `new Address`).
- **Render-verified live (headless)**: installed Playwright chromium; `vite preview` + `?mock`, Chromium at **1280** and **375** for the list, the challenged detail, and the not-found page — screenshots reviewed (parchment canvas, flat hairline cards, ember only on the Challenged chips, grids collapse to 1-col @375, avatars/chips/all sections render) and **console errors = 0** on every page; each page has **exactly one h1**.
- **Verify:** SDK-first build + `pnpm install` green; app `typecheck` clean, `lint` (oxlint) clean, `build` green (`verify-css OK`; the pre-existing `ox` PURE-annotation warnings are non-fatal), `test` = 7 FA2 unit tests still green. Only added `app/src/hooks/*`, `app/src/lib/oracleView.ts`, `app/src/components/oracles/*`, `app/src/data/mockOracles.ts`, rewrote the two stub pages, and added a backward-compatible `as?` prop to `SectionHeader` + wired routes were already present in `main.tsx`. No programs/runner/sdk-src or FA1/FA2 source touched.

## Execution note
Build the SDK first (`pnpm --filter @kassandra/sdk build`) — this slice depends on its decoders/PDAs. FA1 (wallet/provider/shell) + FA2 (data layer + test) are largely INDEPENDENT → can run in parallel (FA1 = providers/shell/NavBar; FA2 = pure data functions + test). FA3 (browse UI) DEPENDS on both (the provider context + the data layer) — sequential after both. Keep the default app build/test offline + green; gate the surfpool integration test. Delphi do/don'ts hold (the `verify-css` guard stays green). Don't touch programs/runner/sdk-src. Append an FA1/FA2/FA3 delta log here.
