# Kassandra dApp — One-Click Settle (derive the settle account set) — Design + Plan

> **For Claude:** REQUIRED SUB-SKILL: subagent-driven-development (per-task implement + review).

**Goal:** Eliminate the last JSON paste in the challenge UI: derive the full `settle_challenge` account set CLIENT-SIDE from the decoded Market + Oracle + PDAs, so **settle becomes a one-click wallet action** like every other flow. NO core (programs/runner/sdk) change (add an app-side derive builder that CONSUMES the SDK). Proven by routing the existing RF4 settle E2E through the derived-account path.

## Feasibility (confirmed — the "not derivable" note was overcautious)
The `settleChallenge` builder (`sdk/src/instructions/challenge.ts:182-240`) derives oracle/market/stakeVault/escrowVault internally from `{oracleNonce, aiClaim}`; the caller supplies 15 accounts, and EVERY one is recoverable:
- **Directly on the decoded Market** (`sdk/src/accounts/market.ts`): `aiClaim`, `proposer`, `question`, `passAmm`, `failAmm`, `kassVault`, `usdcVault`, `oraclePassKass`, `oracleFailKass`, `challenger`, `challengerUsdcVault`.
- **Derived (CU3 already does all of these — reuse `app/src/data/actions/challengeCompose.ts`'s helpers):**
  - `passKassMint`/`failKassMint` = `conditionalTokenMint(market.kassVault, 0/1)` (CU2's `conditionalTokenMint`, pass=0/fail=1).
  - `kassVaultUnderlying` = the KASS conditional vault's underlying token account (CU3 knows this from `composeVault`/`initializeConditionalVault` — the vault-underlying PDA/ATA; confirm the exact derivation from the SDK/compose).
  - `cvEventAuthority` = `futarchy.pda.vaultEventAuthority()` (fixed PDA; CU3 uses it).
  - `proposerUsdc` = `ATA(market.proposer, oracle.usdcMint)`.
  - `challengerUsdcDest` = `ATA(market.challenger, oracle.usdcMint)` (OR `market.challengerUsdcVault` — confirm which the program wants for the DEST vs the escrow; RF4's `buildSettleChallengeIxs` arg names `challengerUsdcDest` vs the escrow `challengerUsdcVault` — get this right from the settle handler/the challenge-market-e2e settle call).
  - `challengerKass` = `ATA(market.challenger, oracle.kassMint)`.
- The `oracleNonce` is the same nonce the finalize/challenge UI already recalls (`app/src/lib/nonceStore.ts` `recallNonce` + the `resolveOracleNonce` scan) — reuse it.

So a `buildSettleFromMarketIxs({ connection, oracleNonce, market, oracle })` can derive all 15 + call `buildSettleChallengeIxs` — no JSON paste.

## Task SD1 — Derive-from-Market settle + one-click UI
- **`app/src/data/actions/challengeSettle.ts`** (pure): `buildSettleFromMarketIxs({ connection?, oracleNonce, market: Market, oracle: Oracle, programId? }): Promise<TransactionInstruction[]>` — derive the 15 accounts (Market fields + the CU2/CU3 derivations above) and call the existing RF4 `buildSettleChallengeIxs` (import-only; DON'T change it). Reuse `conditionalTokenMint` (CU2) + the vault-underlying/event-authority/ATA derivations (CU3's challengeCompose helpers — import or mirror the small ones). Validate that the Market is present + not settled. (connection only if an idempotent ATA-create for a destination ATA is needed — settle likely assumes the dest ATAs exist [the challenger/proposer already hold KASS/USDC]; if a dest ATA could be absent, prepend an idempotent create; confirm from the handler whether settle creates or requires them.)
- **One-click settle UI:** in `app/src/components/oracles/actions/ChallengeTradeControls.tsx` (the CU2 settle control), REPLACE the JSON-paste settle path with the derived one — the settle button (already gated on `now ≥ twapEnd && !settled`, with CU2's verdict preview) now calls `buildSettleFromMarketIxs({ oracleNonce: recallNonce(...)/resolveOracleNonce, market, oracle })` → `useWriteAction` → send. Remove the settle JSON-paste affordance (or keep it as a hidden "advanced" fallback if the derivation could ever miss — but the goal is one-click; prefer removing the paste). Status + refetch. Delphi + a11y unchanged.
- **Offline unit test** (`app/test/challengeSettle.unit.test.ts`): with a decoded-shaped Market + Oracle fixture, assert `buildSettleFromMarketIxs` produces the settle ix whose `data`+`keys` BYTE-MATCH `buildSettleChallengeIxs` called with the hand-supplied equivalent accounts (i.e. the derivation lands the SAME 21-account set) — for known inputs; assert each derived account == its expected PDA/ATA (passKassMint == conditionalTokenMint(kassVault,0), proposerUsdc == ATA(proposer, usdcMint), etc.); validation (settled/no-market rejects). Offline.
- **Gated forked-mainnet E2E:** extend/route RF4's `app/test/challenge.e2e.test.ts` settle (or a sibling) — after the market is composed + the TWAP crossed, drive settle via the NEW `buildSettleFromMarketIxs` (deriving from the decoded on-chain Market, NOT the composed-account JSON) over `keypairSender` → assert the challenge resolves (disqualify/survive) with the same economics RF4 asserts. This proves the DERIVED settle account set is correct against a real Market (the derived accounts == the ones the market was composed with + settle succeeds). Reuse the fixture/harness; only swap the settle ix SOURCE to the derive-from-market builder.
- **Verify:** `pnpm --filter app typecheck` + `pnpm --filter app test` (offline green: the new unit test + existing) + `pnpm --filter app build` (verify-css OK) + `pnpm --filter app lint`. RUN the gated settle E2E. Headless-render the settle control (mock: the one-click settle enabled when twapEnd elapsed + the verdict preview). Update `app/README.md` (settle is now one-click, no JSON paste — the account set is derived from the Market) + the plan/covered-vs-deferred (settle no longer deferred). Commit `feat(app): one-click settle — derive the settle account set from the Market (no JSON paste)`.

## Out of scope / deferred
- Any core (programs/runner/sdk-src) change — the app derives + consumes the SDK builder. If a settle account genuinely ISN'T derivable (recon says all 15 are), STOP-and-report + keep the JSON fallback for that one.
- The compose/open JSON was already removed by CU3; this removes the LAST one (settle). After SD1 the challenge UI has NO JSON paste.

## SD1 delta (implemented)
Settle is no longer deferred — the challenge UI now has NO JSON paste anywhere.
- **`app/src/data/actions/challengeSettle.ts` — `buildSettleFromMarketIxs({ connection?, oracleNonce, market, oracle, proposerAuthority, payer?, programId? })`** derives all 15 caller accounts and calls RF4's `buildSettleChallengeIxs` (import-only):
  - **8 directly off the decoded Market:** `aiClaim`, `proposer`, `question`, `passAmm`, `failAmm`, `kassVault`, `oraclePassKass`, `oracleFailKass`.
  - **7 derived:** `passKassMint`/`failKassMint` = `conditionalTokenMint(market.kassVault, 0/1)`; `kassVaultUnderlying` = `associatedTokenAccount(market.kassVault, oracle.kassMint)`; `cvEventAuthority` = `futarchy.pda.vaultEventAuthority()`; `proposerUsdc` = `ATA(proposerAuthority, oracle.usdcMint)`; `challengerUsdcDest` = `ATA(market.challenger, oracle.usdcMint)`; `challengerKass` = `ATA(market.challenger, oracle.kassMint)`.
- **`challengerUsdcDest` (account 19) vs the escrow (17), RESOLVED:** account 19 is the challenger's OWN USDC ATA — the handler binds it `assert_token_account(.., oracle.usdc_mint, market.challenger)`. The escrow `market.challenger_usdc_vault` is account 17, SDK-derived from the market PDA (NOT supplied). Confirmed against `programs/kassandra/src/processor/settle_challenge.rs`.
- **`proposerUsdc` owner is `Proposer.authority`, NOT the Proposer PDA** (handler asserts `.. proposer.authority`). The Market records only the Proposer PDA, so the builder takes `proposerAuthority` (read off the decoded `Proposer` whose pubkey == `market.proposer`, already in `fetchOracleDetail`). This was the one field not on the Market itself; it IS derivable from the already-fetched proposers, so still no paste.
- **Payout-ATA creates:** settle ASSERTS (never creates) the three payout destinations, so when a `connection` + `payer` are supplied the builder prepends 3 idempotent `createAssociatedTokenAccountIdempotent` ixs (no-op if present); offline it emits the settle ix only.
- **One-click UI:** `ChallengeTradeControls.tsx` — the settle JSON-paste `SettleForm` (textarea) is REPLACED by a one-click `SettleButton` (gating/verdict-preview/a11y unchanged); nonce via `recallNonce`/`resolveOracleNonce`. `OracleDetail.tsx` passes the fetched `proposers` down.
- **Offline byte-match unit test** (`app/test/challengeSettle.unit.test.ts`, 5 tests): the derived settle ix `data`+`keys` BYTE-MATCH `buildSettleChallengeIxs` with the hand-supplied equivalent 21 accounts; each derived account == its expected PDA/ATA; dest != escrow; the 3 ATA-create prepend; validation (settled/no-market/no-authority reject).
- **Gated E2E** (`app/test/challenge.e2e.test.ts`): RF4's settle re-routed through `buildSettleFromMarketIxs`, deriving from the DECODED on-chain Market+Oracle (not the composed JSON); pre-flight asserts the derived keys == the composed accounts, then settle resolves FAIL-side live (disqualify + slash `BOND−kassFee`, escrow→challenger, redeem→stake_vault). PASSES on forked mainnet (~17s).

## Execution note
Build the SDK first. This is a focused single task. REUSE: the decoded Market fields + `buildSettleChallengeIxs` (RF4, import-only) + `conditionalTokenMint` (CU2) + the vault-underlying/event-authority/ATA derivations (CU3's challengeCompose) + `recallNonce`/`resolveOracleNonce`. Get `challengerUsdcDest` vs the escrow `challengerUsdcVault` right (confirm from the settle handler / the e2e settle call). Byte-match the derived settle ix vs a hand-supplied one (the derivation must land the SAME 21 accounts). Prove the derived settle live on forked mainnet (route RF4's settle through it). Delphi/a11y/gating unchanged. Don't touch programs/runner/sdk-src or the RF4/CU2/CU3 builders (import-only). Keep the default suite offline + green; gate the E2E; verify-css green; the challenge UI (CU1/CU2/CU3) + slices 1-4 + Milestone A intact. Append an SD1 delta.
