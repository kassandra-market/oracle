.worktrees/
/target

# surfpool runtime working dirs (logs/state) created by `surfpool start`
.surfpool/
.DS_Store

# Node / pnpm workspace (Kassandra UI — app/)
node_modules/
dist/
dist-ssr/
*.local

# Mintlify docs site (docs-site/) — local CLI cache + local lockfile + build output
.mint/
docs-site/package-lock.json
docs-site/export.zip
docs-site/_site/

# Playwright browser e2e (app/) — funded-wallet secret + run artifacts
app/e2e/.wallet.json
app/e2e/fork/.wallet.json
app/e2e/screenshots/
app/test-results/
app/playwright-report/
app/blob-report/

# Rust indexer build output
indexer/target/
app/e2e/indexer/.wallet.json

# runtime service logs from `make dev` (scripts/dev-full.sh)
/logs/
